<UserControl x:Class="MegaCasting.Wpf.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MegaCasting.Wpf.Views"
        mc:Ignorable="d">
    <Grid ShowGridLines="True" Grid.Row="0" Background="Gray" Visibility="{Binding IsLoggedIn, Converter={StaticResource SharedBoolToVisibilityConverter}, UpdateSourceTrigger=PropertyChanged, ConverterParameter='!'}">
        <Grid ShowGridLines="False">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="150" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="50" />
                <RowDefinition Height="50" />
                <RowDefinition Height="50" />
                <RowDefinition Height="50" />
                <RowDefinition Height="10" />
                <RowDefinition Height="30" />
                <RowDefinition Height="50" />
                <RowDefinition Height="*"  />
            </Grid.RowDefinitions>
            <Label Content="Connexion" Foreground="Black" Grid.Column="2" Grid.Row="1" Style="{StaticResource ConnexionLabel}"></Label>
            <Button x:Name="ConnectButton" Grid.Column="1" Grid.Row="6" Grid.ColumnSpan="2" Content="Connexion" Click="ConnectButton_Click" Style="{StaticResource ConnexionButton}" />
        </Grid>
        <Grid ShowGridLines="False">
           
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="200" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="25" />
                <RowDefinition Height="25"/>
                <RowDefinition Height="25"/>
                <RowDefinition Height="40"/>
                <RowDefinition Height="30"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"  />
            </Grid.RowDefinitions>

            <!--<TextBlock Grid.Column="1" Grid.Row="1"  Text="Login : " Foreground="Black" />
            <TextBlock Grid.Column="1" Grid.Row="2" Text="Mot de passe : " Foreground="Black" />-->

            <TextBox Grid.Column="2" Grid.Row="1" Text="{Binding NomUtilisateur, UpdateSourceTrigger=PropertyChanged}" />
            <PasswordBox x:Name="PasswordBoxLogin" Grid.Column="2" Grid.Row="3" PasswordChanged="PasswordBoxLogin_PasswordChanged" />
            <Label Grid.Column="1" Grid.Row="4" Grid.ColumnSpan="2" Content="{Binding Message}" Foreground="White" Opacity="0.7" HorizontalAlignment="Center" />

        </Grid>
    </Grid>
</UserControl>
